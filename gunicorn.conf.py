bind = "0.0.0.0:8000"  # Bind to all on port 8000
workers = 3  # Number of worker processes
timeout = 30  # Worker timeout in seconds
accesslog = "-"  # Log access to stdout
errorlog = "-"  # Log errors to stdout
